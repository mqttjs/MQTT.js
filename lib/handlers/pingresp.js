export async function handlePingResp () {
  // TODO: In old implementation, pingResp is a state in
  // the client that is toggled to true when the pingResp
  // is received. How does aedes do this? is there a 
  // better way than a boolean toggle?
  return true
}
